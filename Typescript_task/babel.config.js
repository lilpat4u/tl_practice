module.exports = {
  presets: [
    '@babel/preset-env',       // Поддержка современного JavaScript
    '@babel/preset-typescript' // Поддержка TypeScript (если используется)
  ],
  plugins: [
    // Другие плагины, если требуются
  ]
};